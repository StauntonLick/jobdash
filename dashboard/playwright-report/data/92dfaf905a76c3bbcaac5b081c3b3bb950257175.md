# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: search-execution.spec.ts >> Search execution >> does NOT send fullPeriod=true when nothing has changed
- Location: tests/e2e/search-execution.spec.ts:86:7

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - main [ref=e2]:
    - generic [ref=e3]:
      - generic [ref=e5]:
        - generic [ref=e6]:
          - heading "jobbity." [level=1] [ref=e7]
          - button "reset" [ref=e8]
        - generic [ref=e9]:
          - generic [ref=e10]: Find
          - generic [ref=e11]: Search keyword
          - textbox "Search keyword" [ref=e12]:
            - /placeholder: Enter job keywords
          - generic [ref=e13]: jobs in
          - tablist [ref=e15]:
            - tab "Edinburgh (0)" [selected] [ref=e16]
            - button "Add Location" [ref=e17]:
              - img [ref=e18]
              - generic [ref=e19]: Add Location
          - button "Search" [ref=e20]:
            - img
            - generic [ref=e21]: Search
      - tabpanel "Edinburgh (0)" [ref=e24]:
        - generic [ref=e25]:
          - table [ref=e28]:
            - rowgroup [ref=e29]:
              - row "title company Industry Salary Age Status" [ref=e30]:
                - columnheader "title" [ref=e31]
                - columnheader "company" [ref=e32]
                - columnheader "Industry" [ref=e33]
                - columnheader "Salary" [ref=e34]
                - columnheader "Age" [ref=e35]
                - columnheader "Status" [ref=e36]
          - generic [ref=e38]:
            - table [ref=e40]:
              - rowgroup [ref=e41]:
                - row "No jobs found for this search." [ref=e42]:
                  - cell "No jobs found for this search." [ref=e43]
            - paragraph [ref=e44]: 0 jobs hidden by filters
      - complementary [ref=e45]:
        - generic [ref=e47]:
          - button [ref=e49]:
            - img [ref=e50]
          - generic [ref=e53]:
            - generic [ref=e54]:
              - generic [ref=e55]: "-"
              - generic [ref=e57]: "-"
            - generic [ref=e59]:
              - text: Apply
              - img [ref=e60]
  - button "Open Next.js Dev Tools" [ref=e70] [cursor=pointer]:
    - img [ref=e71]
  - alert [ref=e74]
```

# Test source

```ts
  14  |   mockAPIs, goto,
  15  |   EMPTY_CONFIG, CONFIG_WITH_BOTH,
  16  |   MOCK_SEARCH_EMPTY, MOCK_SEARCH_WITH_RESULTS,
  17  | } from "./helpers";
  18  | 
  19  | test.describe("Search execution", () => {
  20  |   test("clicking Search calls /api/searches", async ({ page }) => {
  21  |     let searchCallCount = 0;
  22  |     await mockAPIs(page, {
  23  |       config: CONFIG_WITH_BOTH,
  24  |       searches: [MOCK_SEARCH_EMPTY],
  25  |       onSearchesRequest: () => {
  26  |         searchCallCount++;
  27  |         return [MOCK_SEARCH_EMPTY];
  28  |       },
  29  |     });
  30  |     await goto(page);
  31  | 
  32  |     const before = searchCallCount;
  33  |     await page.locator("#button-search").click();
  34  |     await expect(page.locator("#button-search")).toBeEnabled();
  35  | 
  36  |     expect(searchCallCount).toBeGreaterThan(before);
  37  |   });
  38  | 
  39  |   test("saves config before searching when keyword has changed", async ({ page }) => {
  40  |     const savedBodies: object[] = [];
  41  |     await mockAPIs(page, {
  42  |       config: CONFIG_WITH_BOTH,
  43  |       searches: [MOCK_SEARCH_EMPTY],
  44  |       onConfigMutate: (_method, body) => {
  45  |         savedBodies.push(body);
  46  |         return { ...CONFIG_WITH_BOTH, ...body };
  47  |       },
  48  |     });
  49  |     await goto(page);
  50  | 
  51  |     // Change the keyword — this marks the config as "changed".
  52  |     await page.locator("#input-keyword").fill("Product Designer");
  53  |     await page.locator("#button-search").click();
  54  |     await expect(page.locator("#button-search")).toBeEnabled();
  55  | 
  56  |     expect(savedBodies.length).toBeGreaterThan(0);
  57  |     const patch = savedBodies.find((b) =>
  58  |       Array.isArray((b as Record<string, unknown>).keywords)
  59  |     ) as Record<string, unknown> | undefined;
  60  |     expect(patch?.keywords).toContain("Product Designer");
  61  |   });
  62  | 
  63  |   test("sends fullPeriod=true when config has changed", async ({ page }) => {
  64  |     const searchURLs: string[] = [];
  65  |     await mockAPIs(page, {
  66  |       config: CONFIG_WITH_BOTH,
  67  |       searches: [MOCK_SEARCH_EMPTY],
  68  |       onSearchesRequest: () => [MOCK_SEARCH_EMPTY],
  69  |       onConfigMutate: () => ({ ...CONFIG_WITH_BOTH, keywords: ["Product Designer"] }),
  70  |     });
  71  | 
  72  |     await page.route("/api/searches**", async (route) => {
  73  |       searchURLs.push(route.request().url());
  74  |       await route.fulfill({ json: { searches: [MOCK_SEARCH_EMPTY] } });
  75  |     });
  76  | 
  77  |     await goto(page);
  78  |     await page.locator("#input-keyword").fill("Product Designer");
  79  |     await page.locator("#button-search").click();
  80  |     await expect(page.locator("#button-search")).toBeEnabled();
  81  | 
  82  |     const searchWithFull = searchURLs.find((u) => u.includes("fullPeriod=true"));
  83  |     expect(searchWithFull).toBeDefined();
  84  |   });
  85  | 
  86  |   test("does NOT send fullPeriod=true when nothing has changed", async ({ page }) => {
  87  |     const searchURLs: string[] = [];
  88  | 
  89  |     await mockAPIs(page, { config: CONFIG_WITH_BOTH, searches: [MOCK_SEARCH_EMPTY] });
  90  | 
  91  |     // Capture every search URL (overrides the mockAPIs handler for searches).
  92  |     await page.route("/api/searches**", async (route) => {
  93  |       searchURLs.push(route.request().url());
  94  |       await route.fulfill({ json: { searches: [MOCK_SEARCH_EMPTY] } });
  95  |     });
  96  | 
  97  |     await goto(page);
  98  | 
  99  |     // Wait for the config to have loaded — the tooltip switches from "Add a location"
  100 |     // to the last-updated message once savedConfig is set and searches are present.
  101 |     // Checking the button is enabled (keywordHasValue=true + location loaded) is
  102 |     // sufficient to confirm savedConfig is populated before we click.
  103 |     await expect(page.locator("#button-search")).toBeEnabled();
  104 |     await expect(page.locator("#button-search")).not.toHaveAttribute("title", "Add a location to search");
  105 |     await expect(page.locator("#button-search")).not.toHaveAttribute("title", "Enter a keyword to search");
  106 | 
  107 |     const countBefore = searchURLs.length;
  108 |     await page.locator("#button-search").click();
  109 |     await expect(page.locator("#button-search")).toBeEnabled();
  110 | 
  111 |     // The search triggered by the button should NOT include fullPeriod=true.
  112 |     const buttonURLs = searchURLs.slice(countBefore);
  113 |     expect(buttonURLs.length).toBeGreaterThan(0);
> 114 |     expect(buttonURLs.every((u) => !u.includes("fullPeriod=true"))).toBe(true);
      |                                                                     ^ Error: expect(received).toBe(expected) // Object.is equality
  115 |   });
  116 | 
  117 |   test("draft tabs are cleared once real search results arrive", async ({ page }) => {
  118 |     await mockAPIs(page, {
  119 |       config: { ...EMPTY_CONFIG, keywords: ["UX Designer"] },
  120 |       searches: [],
  121 |       onConfigMutate: (_m, body) => ({ ...EMPTY_CONFIG, keywords: ["UX Designer"], ...body }),
  122 |       onSearchesRequest: () => [MOCK_SEARCH_EMPTY],
  123 |     });
  124 |     await goto(page);
  125 | 
  126 |     // Add a location to create a draft tab.
  127 |     await page.locator("#button-add-tab").click();
  128 |     await page.locator("#dialog-city").fill("Edinburgh");
  129 |     await page.locator("#button-dialog-add-location").click();
  130 |     await expect(page.locator("#dashboard-tab-list")).toContainText("Edinburgh");
  131 | 
  132 |     // Now search — should replace the draft with the real tab.
  133 |     await page.locator("#button-search").click();
  134 |     await expect(page.locator("#button-search")).toBeEnabled();
  135 | 
  136 |     // Real tab still present, no duplicate.
  137 |     const tabs = page.locator("#dashboard-tab-list").getByRole("tab");
  138 |     const tabTexts = await tabs.allInnerTexts();
  139 |     const edinburghTabs = tabTexts.filter((t) => t.includes("Edinburgh"));
  140 |     expect(edinburghTabs).toHaveLength(1);
  141 |   });
  142 | 
  143 |   test("results table renders correct columns", async ({ page }) => {
  144 |     await mockAPIs(page, {
  145 |       config: CONFIG_WITH_BOTH,
  146 |       searches: [MOCK_SEARCH_WITH_RESULTS],
  147 |     });
  148 |     await goto(page);
  149 | 
  150 |     const header = page.locator(`#search-results-header-edinburgh`);
  151 |     await expect(header).toContainText("title");
  152 |     await expect(header).toContainText("company");
  153 |     await expect(header).toContainText("Industry");
  154 |     await expect(header).toContainText("Salary");
  155 |     await expect(header).toContainText("Age");
  156 |     await expect(header).toContainText("Status");
  157 |   });
  158 | 
  159 |   test("results table shows job rows", async ({ page }) => {
  160 |     await mockAPIs(page, {
  161 |       config: CONFIG_WITH_BOTH,
  162 |       searches: [MOCK_SEARCH_WITH_RESULTS],
  163 |     });
  164 |     await goto(page);
  165 | 
  166 |     const firstRow = page.locator("#search-result-row-edinburgh-0");
  167 |     await expect(firstRow).toContainText("Senior UX Designer");
  168 |     await expect(firstRow).toContainText("Acme Corp");
  169 |   });
  170 | 
  171 |   test("empty state shown when search returns no jobs", async ({ page }) => {
  172 |     await mockAPIs(page, {
  173 |       config: CONFIG_WITH_BOTH,
  174 |       searches: [MOCK_SEARCH_EMPTY],
  175 |     });
  176 |     await goto(page);
  177 | 
  178 |     await expect(page.locator("#search-results-empty-row-edinburgh")).toContainText(
  179 |       "No jobs found for this search."
  180 |     );
  181 |   });
  182 | 
  183 |   test("tab label shows result count", async ({ page }) => {
  184 |     await mockAPIs(page, {
  185 |       config: CONFIG_WITH_BOTH,
  186 |       searches: [MOCK_SEARCH_WITH_RESULTS],
  187 |     });
  188 |     await goto(page);
  189 | 
  190 |     // Tab trigger contains the title and count in parentheses.
  191 |     await expect(page.locator("#tab-trigger-edinburgh")).toContainText("Edinburgh (1)");
  192 |   });
  193 | 
  194 |   test("keyword input is populated from saved config on load", async ({ page }) => {
  195 |     await mockAPIs(page, {
  196 |       config: CONFIG_WITH_BOTH,
  197 |       searches: [MOCK_SEARCH_EMPTY],
  198 |     });
  199 |     await goto(page);
  200 | 
  201 |     await expect(page.locator("#input-keyword")).toHaveValue("UX Designer");
  202 |   });
  203 | });
  204 | 
```