# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: search-execution.spec.ts >> Search execution >> keyword input is populated from saved config on load
- Location: tests/e2e/search-execution.spec.ts:194:7

# Error details

```
Error: expect(locator).toHaveValue(expected) failed

Locator:  locator('#input-keyword')
Expected: "UX Designer"
Received: ""
Timeout:  5000ms

Call log:
  - Expect "toHaveValue" with timeout 5000ms
  - waiting for locator('#input-keyword')
    14 × locator resolved to <input type="text" id="input-keyword" placeholder="Enter job keywords" class="h-9 w-[240px] shrink-0 rounded-full border border-black/8 bg-control-strong px-4 text-sm text-control-foreground placeholder:text-muted-foreground hover:bg-control-solid focus:bg-control-solid focus:border-black/32 focus:outline-none focus:ring-0"/>
       - unexpected value ""

```

```yaml
- textbox "Search keyword":
  - /placeholder: Enter job keywords
```

# Test source

```ts
  101 |     // Checking the button is enabled (keywordHasValue=true + location loaded) is
  102 |     // sufficient to confirm savedConfig is populated before we click.
  103 |     await expect(page.locator("#button-search")).toBeEnabled();
  104 |     await expect(page.locator("#button-search")).not.toHaveAttribute("title", "Add a location to search");
  105 |     await expect(page.locator("#button-search")).not.toHaveAttribute("title", "Enter a keyword to search");
  106 | 
  107 |     const countBefore = searchURLs.length;
  108 |     await page.locator("#button-search").click();
  109 |     await expect(page.locator("#button-search")).toBeEnabled();
  110 | 
  111 |     // The search triggered by the button should NOT include fullPeriod=true.
  112 |     const buttonURLs = searchURLs.slice(countBefore);
  113 |     expect(buttonURLs.length).toBeGreaterThan(0);
  114 |     expect(buttonURLs.every((u) => !u.includes("fullPeriod=true"))).toBe(true);
  115 |   });
  116 | 
  117 |   test("draft tabs are cleared once real search results arrive", async ({ page }) => {
  118 |     await mockAPIs(page, {
  119 |       config: { ...EMPTY_CONFIG, keywords: ["UX Designer"] },
  120 |       searches: [],
  121 |       onConfigMutate: (_m, body) => ({ ...EMPTY_CONFIG, keywords: ["UX Designer"], ...body }),
  122 |       onSearchesRequest: () => [MOCK_SEARCH_EMPTY],
  123 |     });
  124 |     await goto(page);
  125 | 
  126 |     // Add a location to create a draft tab.
  127 |     await page.locator("#button-add-tab").click();
  128 |     await page.locator("#dialog-city").fill("Edinburgh");
  129 |     await page.locator("#button-dialog-add-location").click();
  130 |     await expect(page.locator("#dashboard-tab-list")).toContainText("Edinburgh");
  131 | 
  132 |     // Now search — should replace the draft with the real tab.
  133 |     await page.locator("#button-search").click();
  134 |     await expect(page.locator("#button-search")).toBeEnabled();
  135 | 
  136 |     // Real tab still present, no duplicate.
  137 |     const tabs = page.locator("#dashboard-tab-list").getByRole("tab");
  138 |     const tabTexts = await tabs.allInnerTexts();
  139 |     const edinburghTabs = tabTexts.filter((t) => t.includes("Edinburgh"));
  140 |     expect(edinburghTabs).toHaveLength(1);
  141 |   });
  142 | 
  143 |   test("results table renders correct columns", async ({ page }) => {
  144 |     await mockAPIs(page, {
  145 |       config: CONFIG_WITH_BOTH,
  146 |       searches: [MOCK_SEARCH_WITH_RESULTS],
  147 |     });
  148 |     await goto(page);
  149 | 
  150 |     const header = page.locator(`#search-results-header-edinburgh`);
  151 |     await expect(header).toContainText("title");
  152 |     await expect(header).toContainText("company");
  153 |     await expect(header).toContainText("Industry");
  154 |     await expect(header).toContainText("Salary");
  155 |     await expect(header).toContainText("Age");
  156 |     await expect(header).toContainText("Status");
  157 |   });
  158 | 
  159 |   test("results table shows job rows", async ({ page }) => {
  160 |     await mockAPIs(page, {
  161 |       config: CONFIG_WITH_BOTH,
  162 |       searches: [MOCK_SEARCH_WITH_RESULTS],
  163 |     });
  164 |     await goto(page);
  165 | 
  166 |     const firstRow = page.locator("#search-result-row-edinburgh-0");
  167 |     await expect(firstRow).toContainText("Senior UX Designer");
  168 |     await expect(firstRow).toContainText("Acme Corp");
  169 |   });
  170 | 
  171 |   test("empty state shown when search returns no jobs", async ({ page }) => {
  172 |     await mockAPIs(page, {
  173 |       config: CONFIG_WITH_BOTH,
  174 |       searches: [MOCK_SEARCH_EMPTY],
  175 |     });
  176 |     await goto(page);
  177 | 
  178 |     await expect(page.locator("#search-results-empty-row-edinburgh")).toContainText(
  179 |       "No jobs found for this search."
  180 |     );
  181 |   });
  182 | 
  183 |   test("tab label shows result count", async ({ page }) => {
  184 |     await mockAPIs(page, {
  185 |       config: CONFIG_WITH_BOTH,
  186 |       searches: [MOCK_SEARCH_WITH_RESULTS],
  187 |     });
  188 |     await goto(page);
  189 | 
  190 |     // Tab trigger contains the title and count in parentheses.
  191 |     await expect(page.locator("#tab-trigger-edinburgh")).toContainText("Edinburgh (1)");
  192 |   });
  193 | 
  194 |   test("keyword input is populated from saved config on load", async ({ page }) => {
  195 |     await mockAPIs(page, {
  196 |       config: CONFIG_WITH_BOTH,
  197 |       searches: [MOCK_SEARCH_EMPTY],
  198 |     });
  199 |     await goto(page);
  200 | 
> 201 |     await expect(page.locator("#input-keyword")).toHaveValue("UX Designer");
      |                                                  ^ Error: expect(locator).toHaveValue(expected) failed
  202 |   });
  203 | });
  204 | 
```